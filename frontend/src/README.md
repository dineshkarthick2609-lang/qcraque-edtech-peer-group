# EduConnect Frontend

The EduConnect frontend is built using React and TypeScript.

## Features

- Fetches student data from the FastAPI backend
- Displays students in a responsive card layout
- Shows loading state
- Handles API errors
- Displays empty-state information
- Uses external CSS for styling

## API Integration

The frontend communicates with:

```text
GET http://localhost:8000/students
